import type { NextPage } from "next";
import { Layout } from "components/scaffold";

const CategoryNewPage: NextPage = () => {
  return (
    <Layout>
    </Layout>
  );
};

export default CategoryNewPage;
